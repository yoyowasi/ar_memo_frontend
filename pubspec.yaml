name: ar_memo_frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # SDK 버전 명시

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # 네트워킹
  http: ^1.2.1

  # 상태 관리
  flutter_riverpod: ^2.5.1

  # 데이터 저장
  shared_preferences: ^2.2.3

  # 환경 변수
  flutter_dotenv: ^5.1.0

  # 유틸리티
  intl: ^0.19.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    # - assets/kakao_icon.png # 카카오 아이콘 사용 시 주석 해제