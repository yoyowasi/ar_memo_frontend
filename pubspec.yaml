name: ar_memo_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0' # 프로젝트 SDK 버전에 맞게 조정

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  flutter_speed_dial: ^7.0.0

  # --- AR 패키지 (신버전 'updated'로 통일) ---
  ar_flutter_plugin: ^0.7.3 # <- 삭제
  # ar_flutter_plugin_updated: ^1.0.1 # <- 신버전 사용 (또는 최신 버전)
  # ---------------------------------

  # --- 카카오맵 SDK (공식 SDK 유지) ---
  kakao_map_plugin: ^0.3.7
  # -------------------------------

  # 네트워킹 & 상태관리 등
  http: ^1.2.1
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  vector_math: ^2.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.1
  riverpod_lint: ^2.6.5
  custom_lint: ^0.7.6

flutter:
  uses-material-design: true

  assets:
    - .env
    # --- 에셋 경로 수정 ('assets/' 제거) ---
    - Models/
    # -----------------------------------