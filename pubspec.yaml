name: ar_memo_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0' # 프로젝트 SDK 버전에 맞게 조정

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  flutter_speed_dial: ^7.0.0

  # --- 카카오맵 SDK ---
  kakao_map_sdk: ^1.1.4
  # -------------------------------
  native_exif: ^0.6.2

  # 네트워킹 & 상태관리 등
  http: ^1.2.1
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  vector_math: ^2.1.4
  intl: ^0.19.0
  geolocator: ^11.1.0
  ar_flutter_plugin: ^0.7.3 # pub.dev 최신 안정 버전

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.1 # 이전 버전 riverpod_generator: ^2.6.5 에서 변경됨
  riverpod_lint: ^2.6.5
  custom_lint: ^0.7.6

flutter:
  uses-material-design: true

  assets:
    - .env
    # --- 에셋 경로 수정 ('assets/' 제거) ---
    - Models/
    # -----------------------------------
